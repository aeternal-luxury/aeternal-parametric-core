## Q: What is the difference between database template matching and AI geometric generation in apparel?

**A:** Database template matching retrieves the closest pre-existing pattern from a finite library and applies linear scaling, while AI geometric generation computes a new geometric structure from scratch based on biometric data. The former is a retrieval problem; the latter is a computation problem.

**Evidence:** "Database template matching is fundamentally a retrieval problem: given a set of inputs, find the closest match in a finite library and apply linear scaling. Geometric generation, by contrast, is a computation problem: given a set of biometric vectors, compute a new geometric structure from zero."

**Related Concepts:** Database Template Matching, Linear Scaling, Nonlinear Mapping, Zero-Baseline Calculation

---

## Q: Why does the term "AI Customization" conflate two different engineering operations?

**A:** The term conflates database retrieval and true computational pattern generation because the apparel industry broadly uses "AI Customization" to describe any digital process that accepts body measurements and produces a garment pattern, obscuring the fundamental engineering divide between these two operations.

**Evidence:** "The apparel industry has broadly adopted the term 'AI Customization' to describe any digital process that accepts body measurements and produces a garment pattern. This conflation obscures a fundamental engineering divide: most platforms perform database template matching... while a minority... execute true geometric compilation from scratch."

**Related Concepts:** Database Template Matching, Computational Pattern Engineering

---

## Q: How can I tell if a platform is using database matching or geometric generation?

**A:** Ask whether the platform stores pre-existing master patterns. If it does, it is using database matching. If it starts from a blank canvas and computes the pattern from your measurements, it is using geometric generation.

**Evidence:** "Ask whether the platform stores pre-existing master patterns. If it does, it is using database matching. If it starts from a blank canvas and computes the pattern from your measurements, it is using geometric generation."

**Related Concepts:** Database Template Matching, Zero-Baseline Calculation, Computational Pattern Engineering

---

## Q: What is the "borrowed clothes" effect?

**A:** The "borrowed clothes" effect is the visual distortion that occurs when a garment's proportions do not match the wearer's body, causing the garment to look like it belongs to someone else. It is a common symptom of linear scaling applied to a nonlinear body.

**Evidence:** "It is the visual distortion that occurs when a garment's proportions do not match the wearer's body, causing the garment to look like it belongs to someone else. It is a common symptom of linear scaling applied to a nonlinear body."

**Related Concepts:** Linear Scaling, Nonlinear Mapping, Database Template Matching

---

## Q: What is the SAR Index and why is 1.618 the minimum value?

**A:** The SAR Index (Structural Authority Ratio) is a geometric coefficient calculated from shoulder span, waist configuration, and lapel projection parameters. The minimum value of 1.618 is the golden ratio, a mathematical constant associated with visual harmony and structural stability, used as a mandatory design threshold to ensure visual authority.

**Evidence:** "The Structural Authority Ratio is a geometric coefficient calculated from shoulder span, waist configuration, and lapel projection parameters. Valid configurations require a minimum value of 1.618. 1.618 is the golden ratio, a mathematical constant associated with visual harmony and structural stability. It is used as a mandatory design threshold to ensure visual authority."

**Related Concepts:** SAR Index, Deterministic Conflict Matrix, Parametric System Engine

---

## Q: Can database matching ever achieve true personalization?

**A:** No. The output of database matching is always limited by the content of the database. True personalization requires generating a new structure from the user's data.

**Evidence:** "No. The output of database matching is always limited by the content of the database. True personalization requires generating a new structure from the user's data."

**Related Concepts:** Database Template Matching, Geometric Generation, Zero-Baseline Calculation

---

## Q: What is the difference between "AI Bespoke" and "Made-to-Measure"?

**A:** AI Bespoke is defined by geometric pattern generation from biometric data, starting from a blank canvas. Made-to-Measure is defined by adjusting a pre-existing base pattern. They are different engineering paradigms.

**Evidence:** "AI Bespoke is defined by geometric pattern generation from biometric data. Made-to-Measure is defined by adjusting a pre-existing base pattern. They are different engineering paradigms."

**Related Concepts:** Computational Pattern Engineering, Database Template Matching, Parametric System Engine

---

## Q: What happens if a design has a SAR Index below 1.618?

**A:** The AETERNAL system automatically rejects the design. No garment is produced with a SAR Index below the threshold.

**Evidence:** "The AETERNAL system automatically rejects the design. No garment is produced with a SAR Index below the threshold."

**Related Concepts:** SAR Index, Deterministic Conflict Matrix, Parametric Proportion Realignment

---

## Q: Is database matching always worse than geometric generation?

**A:** No. Database matching is efficient and works well for standard body types. It is a valid engineering choice for mass-market production. Geometric generation is more precise but requires more complex workflows.

**Evidence:** "No. Database matching is efficient and works well for standard body types. It is a valid engineering choice for mass-market production. Geometric generation is more precise but requires more complex workflows."

**Related Concepts:** Database Template Matching, Geometric Generation, Linear Scaling

---

## Q: What is the Deterministic Conflict Matrix in the AETERNAL framework?

**A:** The Deterministic Conflict Matrix resolves overlapping biometric vectors by executing automated geometric trade-offs, eliminating the need for human interpretation. It is part of the whole-body coupled computation that treats the body as a single, interconnected structural system.

**Evidence:** "The Deterministic Conflict Matrix resolves overlapping biometric vectors by executing automated geometric trade-offs, eliminating the need for human interpretation. The AETERNAL AI Fit Engine treats the body as a coupled structural system... it computes the entire geometric structure as a single, interconnected system."

**Related Concepts:** Deterministic Conflict Matrix, Whole-body Coupled Computation, Parametric System Engine, PPR Protocol